# bukken-pipeline 環境変数の雛形
# コピーして .env を作成し、実際の値を入れる。.env はコミットしないこと（.gitignore済み）。

# --- 不動産情報ライブラリ API（国交省）---
# 申請: https://www.reinfolib.mlit.go.jp/api/request/
REINFOLIB_API_KEY=

# --- Google Maps Routes API（通勤時間の実測）---
# 有効化: https://console.cloud.google.com/apis/library/routes.googleapis.com
#   （旧Directions APIは2025年3月以降の新規プロジェクトでは有効化できないため、
#    Routes API を使うこと。有効化にはGoogle Cloud課金アカウント登録が必要だが、
#    月10,000コールまで無料でこのツールの利用量なら課金は発生しない見込み）
# キー取得: https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API_KEY=

# --- Gmail API / Google Sheets API（共通のOAuthクライアントを使う）---
# 取得: https://console.cloud.google.com/apis/credentials （OAuth 2.0 クライアントID・デスクトップアプリ）
# ダウンロードした client_secret.json のパスを指定する（値そのものはコミットしない）
# 初回実行時にブラウザで認可すると GMAIL_TOKEN_PATH にトークンが保存され、
# Gmail読み取り・送信・Sheets書き込みをまとめて使えるようになる。
GMAIL_OAUTH_CLIENT_SECRET_PATH=./secrets/gmail_client_secret.json
GMAIL_TOKEN_PATH=./secrets/gmail_token.json
GMAIL_LABEL=bukken

# --- Google Sheets 出力（任意・M5）---
GOOGLE_SHEETS_SPREADSHEET_ID=

# --- 通知（任意・M5）---
# 新規の高得点候補が出たときにGmail API経由でメール送信する。
NOTIFY_EMAIL_TO=
NOTIFY_SCORE_THRESHOLD=75
# LINE Notifyは2025年3月31日にサービス終了済みのため未実装（README参照）。
